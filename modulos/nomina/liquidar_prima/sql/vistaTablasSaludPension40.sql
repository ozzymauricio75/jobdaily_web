    -- Campos necesarios de cada tabla para esta operacion --

    -- documento_identidad_empleado
    -- codigo_empresa
    -- fecha_ingreso_empresa
    -- codigo_sucursal
    -- fecha_ingreso_sucursal
    -- codigo_transaccion_contable
    -- sentido
    -- ano_generacion
    -- mes_generacion
    -- periodo_pago
    -- fecha_pago_planilla
    -- codigo_planilla
    -- valor_movimiento
    -- contabilizado

    CREATE OR REPLACE ALGORITHM = MERGE VIEW job_tablas_salud_pension_extras AS
    (
        SELECT
        FROM
        WHERE
    )
    UNION ALL
    (
        SELECT
        FROM
        WHERE
    )
